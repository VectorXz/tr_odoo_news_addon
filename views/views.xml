<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="news.list">
      <field name="name">news list</field>
      <field name="model">news.news</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="news.action_window">
      <field name="name">news window</field>
      <field name="res_model">news.news</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="news.action_server">
      <field name="name">news server</field>
      <field name="model_id" ref="model_news_news"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="news" id="news.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="news.menu_1" parent="news.menu_root"/>
    <menuitem name="Menu 2" id="news.menu_2" parent="news.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="news.menu_1_list" parent="news.menu_1"
              action="news.action_window"/>
    <menuitem name="Server to list" id="news" parent="news.menu_2"
              action="news.action_server"/>
    -->
  </data>
</odoo>